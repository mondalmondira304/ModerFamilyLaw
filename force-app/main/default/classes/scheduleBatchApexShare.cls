public class scheduleBatchApexShare{}
//global class scheduleBatchApexShare implements Schedulable {
    // global void execute(SchedulableContext sc) {
    //     batchApexShares bShares = new batchApexShares();
    //     bShares.query = 'SELECT ID,Client_Name__c FROM Matters__c';
    //     database.executeBatch(bShares);	
    //  }
//}